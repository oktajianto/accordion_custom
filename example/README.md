# accordion_custom example

A runnable gallery for [`accordion_custom`](https://pub.dev/packages/accordion_custom).
Each card demonstrates one feature: single/multiple modes, a controller,
custom styling, nesting, and the `.builder` constructor.

```bash
cd example
flutter create .   # regenerate the platform folders once
flutter run
```
