# Screenshots

Upload the demo GIFs here using exactly these filenames (referenced from the
root `README.md`):

| File | Feature section |
| --- | --- |
| `basic.gif` | Basic usage (single mode) |
| `multiple.gif` | Multiple open panels |
| `builder.gif` | Building from a data list |
| `controller.gif` | Programmatic control |
| `styling.gif` | Styling (colors, border & radius) |
| `per-panel.gif` | Per-panel styling |
| `divider.gif` | Divider between header and content |
| `padding.gif` | Custom padding |
| `custom-header.gif` | Custom header |
| `icon.gif` | Custom or hidden icon |
| `disabled.gif` | Disabled panels |
| `initial-state.png` | Initial state |
| `animation.gif` | Animation |
| `nesting.gif` | Nesting |

Tip: keep each GIF reasonably small (a few hundred KB) so the README stays light
on pub.dev. Once a file is added here, its placeholder in the README renders
automatically.
